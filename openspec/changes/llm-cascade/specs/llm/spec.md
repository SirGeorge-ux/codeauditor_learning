# llm Specification (delta for llm-cascade)

> **Estado:** esqueleto pendiente de rellenar tras el design.
> **Esta spec es un delta sobre:** `openspec/specs/llm/spec.md` (si existe).

## Purpose

(pendiente — 1 párrafo: qué define esta capability)

## ADDED Requirements

### Requirement: (pendiente)

The system MUST / SHOULD / MAY ...

#### Scenario: (pendiente)

- GIVEN (estado inicial)
- WHEN (acción)
- THEN (resultado esperado)

## MODIFIED Requirements

### Requirement: (pendiente — solo si se modifica una existente)

(pendiente — diff con ## antes / después)

## REMOVED Requirements

### Requirement: (pendiente — solo si se elimina una existente)

(pendiente — razón de la eliminación)
